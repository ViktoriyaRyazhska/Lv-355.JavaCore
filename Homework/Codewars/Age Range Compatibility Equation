public class Kata{
  public static String datingRange(int age) {
    if (age > 14) {
        return (age / 2 + 7) + "-" + ((age - 7) * 2);
    }
    int min = (int) Math.round(age - 0.10 * age);
    int max = (int) Math.round(age + 0.10 * age);

    return min + "-" + max;
  }
}
